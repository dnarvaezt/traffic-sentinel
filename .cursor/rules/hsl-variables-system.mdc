---
alwaysApply: true
---

# Sistema de Diseño Centralizado de shadcn/Tailwind

## Regla Principal

Este proyecto utiliza el **sistema de diseño completo de shadcn/Tailwind** como fuente de verdad única para todos los aspectos visuales y de espaciado.

**PROHIBIDO** crear valores hardcodeados o variables personalizadas sin validación previa y justificación explícita.

---

## 🎯 Filosofía del Sistema

### Principios Core

1. **Centralización**: Un solo lugar define cada aspecto del diseño
2. **Consistencia**: Todos los componentes usan las mismas variables y tokens
3. **Dinamismo**: Los temas pueden cambiar sin modificar componentes
4. **Reutilización**: Maximizar uso de utilidades existentes antes de crear nuevas
5. **Escalabilidad**: Fácil mantener y extender sin comprometer el sistema

---

## 📐 Categorías del Sistema de Diseño

### 1. 🎨 Colores (Variables HSL)

#### Variables Protegidas de shadcn/Tailwind

```css
/* Variables Semánticas de Color (NUNCA duplicar) */
--background          /* Fondo principal de la aplicación */
--foreground          /* Texto principal sobre background */
--card                /* Fondo de tarjetas y contenedores */
--card-foreground     /* Texto sobre card */
--popover             /* Fondo de popovers y tooltips */
--popover-foreground  /* Texto sobre popover */
--primary             /* Color primario de marca */
--primary-foreground  /* Texto sobre primary */
--secondary           /* Color secundario */
--secondary-foreground /* Texto sobre secondary */
--muted               /* Elementos deshabilitados o secundarios */
--muted-foreground    /* Texto sobre muted */
--accent              /* Color de acento para highlights */
--accent-foreground   /* Texto sobre accent */
--destructive         /* Acciones destructivas (eliminar, error) */
--destructive-foreground /* Texto sobre destructive */
--border              /* Color de bordes */
--input               /* Bordes de inputs */
--ring                /* Anillos de foco (focus-visible) */
```

#### Reglas de Uso de Colores

**✅ CORRECTO:**

```scss
// Uso directo de variables
.component {
  background-color: hsl(var(--background));
  color: hsl(var(--foreground));
  border: 1px solid hsl(var(--border));
}

// Con opacidad (no crear nueva variable)
.overlay {
  background-color: hsl(var(--background) / 0.8);
  backdrop-filter: blur(10px);
}

// En Tailwind
<div className="bg-background text-foreground border border-border">
```

**❌ PROHIBIDO:**

```scss
// Valores hex/rgb directos
.component {
  background-color: #ffffff;  // ❌ Usar hsl(var(--background))
  color: rgb(0, 0, 0);        // ❌ Usar hsl(var(--foreground))
}

// Duplicar variables existentes
:root {
  --my-bg: 0 0% 100%;         // ❌ Ya existe --background
  --custom-text: 0 0% 0%;     // ❌ Ya existe --foreground
}

// Clases de Tailwind hardcoded
<div className="bg-white text-black">  // ❌ Usar bg-background text-foreground
```

---

### 2. 🔤 Tipografía

#### Variables de Fuentes (Tailwind Default)

**Usar clases de Tailwind, NO valores hardcodeados:**

```scss
// ❌ PROHIBIDO: Valores hardcodeados
.title {
  font-family: 'Inter', sans-serif;  // ❌
  font-size: 24px;                   // ❌
  font-weight: 700;                  // ❌
  line-height: 1.5;                  // ❌
}

// ✅ CORRECTO: Clases de Tailwind
.title {
  @apply font-sans text-2xl font-bold leading-normal;
}

// O en JSX
<h1 className="font-sans text-2xl font-bold leading-normal">
```

#### Sistema de Tamaños de Fuente (Tailwind Scale)

| Clase Tailwind | Tamaño | Uso Semántico |
|----------------|--------|---------------|
| `text-xs` | 0.75rem (12px) | Metadatos, disclaimers |
| `text-sm` | 0.875rem (14px) | Texto secundario, subtítulos |
| `text-base` | 1rem (16px) | Cuerpo de texto estándar |
| `text-lg` | 1.125rem (18px) | Texto destacado |
| `text-xl` | 1.25rem (20px) | Títulos pequeños |
| `text-2xl` | 1.5rem (24px) | Títulos de sección |
| `text-3xl` | 1.875rem (30px) | Títulos principales |
| `text-4xl` | 2.25rem (36px) | Títulos de página |
| `text-5xl` | 3rem (48px) | Héroes, títulos grandes |
| `text-6xl` | 3.75rem (60px) | Display, landing pages |

#### Pesos de Fuente (Tailwind)

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.light   { @apply font-light; }      // 300
.normal  { @apply font-normal; }     // 400
.medium  { @apply font-medium; }     // 500
.semibold { @apply font-semibold; }  // 600
.bold    { @apply font-bold; }       // 700
.extrabold { @apply font-extrabold; } // 800
.black   { @apply font-black; }      // 900

// ❌ PROHIBIDO: Valores numéricos directos
.title {
  font-weight: 700;  // ❌ Usar font-bold
}
```

---

### 3. 📏 Espaciado (Spacing Scale)

**Sistema de espaciado de Tailwind (basado en 0.25rem = 4px):**

#### Escala de Espaciado

| Clase | Valor | Píxeles | Uso Común |
|-------|-------|---------|-----------|
| `0` | 0 | 0px | Sin espacio |
| `px` | 1px | 1px | Bordes finos |
| `0.5` | 0.125rem | 2px | Espacios mínimos |
| `1` | 0.25rem | 4px | Espacios muy pequeños |
| `2` | 0.5rem | 8px | Espacios pequeños |
| `3` | 0.75rem | 12px | Espacios compactos |
| `4` | 1rem | 16px | Espacios estándar |
| `5` | 1.25rem | 20px | Espacios medios |
| `6` | 1.5rem | 24px | Espacios amplios |
| `8` | 2rem | 32px | Separación de secciones |
| `10` | 2.5rem | 40px | Espacios grandes |
| `12` | 3rem | 48px | Espacios muy grandes |
| `16` | 4rem | 64px | Espacios extra grandes |
| `20` | 5rem | 80px | Espacios masivos |

#### Reglas de Uso

```scss
// ✅ CORRECTO: Clases de Tailwind
.card {
  @apply p-4 m-2 gap-3;           // padding, margin, gap
  @apply space-y-4;                // espacio vertical entre hijos
  @apply px-6 py-3;                // padding horizontal y vertical
}

// ❌ PROHIBIDO: Valores hardcodeados
.card {
  padding: 16px;                   // ❌ Usar p-4
  margin: 8px;                     // ❌ Usar m-2
  gap: 12px;                       // ❌ Usar gap-3
}

// ❌ PROHIBIDO: Valores personalizados sin justificación
.card {
  padding: 18px;                   // ❌ No está en la escala de Tailwind
  margin: 13px;                    // ❌ Valor arbitrario
}
```

---

### 4. 🔲 Bordes

#### Anchos de Borde (Tailwind)

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.element {
  @apply border;      // 1px
  @apply border-2;    // 2px
  @apply border-4;    // 4px
  @apply border-8;    // 8px
}

// Bordes específicos por lado
.element {
  @apply border-t;    // top
  @apply border-r-2;  // right 2px
  @apply border-b-4;  // bottom 4px
  @apply border-l;    // left
}

// ❌ PROHIBIDO: Valores directos
.element {
  border-width: 3px;  // ❌ No está en escala de Tailwind
  border-top: 5px solid;  // ❌ Usar border-t-[5px] si es necesario
}
```

#### Colores de Borde

```scss
// ✅ CORRECTO: Usar variables semánticas
.element {
  @apply border border-border;           // Borde estándar
  @apply border-2 border-primary;        // Borde primario
  @apply border border-destructive;      // Borde de error
  @apply border border-muted;            // Borde suave
}

// ❌ PROHIBIDO: Colores hardcoded
.element {
  border-color: #e5e7eb;  // ❌ Usar border-border
  border: 1px solid gray; // ❌ Usar border border-border
}
```

---

### 5. 🔘 Border Radius (Redondeo)

#### Variable de shadcn

```css
:root {
  --radius: 0.5rem;  /* Variable base de shadcn (NO modificar directamente) */
}
```

#### Clases de Tailwind (basadas en `--radius`)

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.element {
  @apply rounded-none;   // 0
  @apply rounded-sm;     // 0.125rem (2px)
  @apply rounded;        // 0.25rem (4px)
  @apply rounded-md;     // 0.375rem (6px)
  @apply rounded-lg;     // 0.5rem (8px) - valor por defecto de --radius
  @apply rounded-xl;     // 0.75rem (12px)
  @apply rounded-2xl;    // 1rem (16px)
  @apply rounded-3xl;    // 1.5rem (24px)
  @apply rounded-full;   // 9999px (círculo perfecto)
}

// Redondeo específico por esquina
.element {
  @apply rounded-t-lg;      // top
  @apply rounded-r-xl;      // right
  @apply rounded-b-2xl;     // bottom
  @apply rounded-l-lg;      // left
  @apply rounded-tl-lg;     // top-left
  @apply rounded-tr-xl;     // top-right
}

// ❌ PROHIBIDO: Valores hardcodeados
.element {
  border-radius: 8px;           // ❌ Usar rounded-lg
  border-radius: 16px;          // ❌ Usar rounded-2xl
  border-top-left-radius: 12px; // ❌ Usar rounded-tl-xl
}
```

---

### 6. 🌑 Sombras (Shadows)

#### Sistema de Sombras de Tailwind

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.element {
  @apply shadow-sm;      // Sombra sutil
  @apply shadow;         // Sombra estándar
  @apply shadow-md;      // Sombra media
  @apply shadow-lg;      // Sombra grande
  @apply shadow-xl;      // Sombra extra grande
  @apply shadow-2xl;     // Sombra masiva
  @apply shadow-inner;   // Sombra interior
  @apply shadow-none;    // Sin sombra
}

// Sombras con variables de color
.element {
  // Sombra que se adapta al tema
  box-shadow: 0 4px 12px hsl(var(--foreground) / 0.1);

  // Sombras múltiples
  box-shadow:
    0 1px 2px hsl(var(--foreground) / 0.05),
    0 4px 8px hsl(var(--foreground) / 0.1);
}

// ❌ PROHIBIDO: Sombras hardcodeadas
.element {
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);  // ❌ Usar shadow-md o variables HSL
  box-shadow: 2px 2px 10px #000;             // ❌ Usar shadow-lg o variables HSL
}

// Text Shadow con variables HSL
.title {
  text-shadow: 0 2px 4px hsl(var(--foreground) / 0.1);  // ✅
}
```

---

### 7. ⚡ Transiciones y Animaciones

#### Duraciones de Transición (Tailwind)

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.element {
  @apply transition-all;           // todas las propiedades
  @apply transition-colors;        // solo colores
  @apply transition-transform;     // solo transform
  @apply transition-opacity;       // solo opacity

  // Duraciones
  @apply duration-75;    // 75ms
  @apply duration-100;   // 100ms
  @apply duration-150;   // 150ms - rápido
  @apply duration-200;   // 200ms - estándar
  @apply duration-300;   // 300ms - medio
  @apply duration-500;   // 500ms - lento
  @apply duration-700;   // 700ms - muy lento

  // Easing functions
  @apply ease-linear;
  @apply ease-in;
  @apply ease-out;
  @apply ease-in-out;
}

// ❌ PROHIBIDO: Duraciones hardcodeadas
.element {
  transition: all 250ms ease;  // ❌ Usar duration-200 o duration-300
  transition-duration: 0.4s;   // ❌ Usar duration-500
}
```

#### Animaciones Predefinidas

```scss
// ✅ CORRECTO: Animaciones de Tailwind
.element {
  @apply animate-spin;       // Girar continuamente
  @apply animate-ping;       // Pulso que crece
  @apply animate-pulse;      // Fade in/out
  @apply animate-bounce;     // Rebote
}

// Animación personalizada (solo si es necesaria)
@keyframes shimmer {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

.element {
  animation: shimmer 2s ease-in-out infinite;
}
```

---

### 8. 📱 Breakpoints (Responsive)

#### Breakpoints de Tailwind (NO modificar)

```scss
// Sistema de breakpoints de Tailwind
sm:  640px   // Teléfonos grandes
md:  768px   // Tablets
lg:  1024px  // Laptops
xl:  1280px  // Desktops
2xl: 1536px  // Pantallas grandes

// ✅ CORRECTO: Usar prefijos responsive
.element {
  @apply w-full;           // Mobile first
  @apply md:w-1/2;         // 50% en tablets+
  @apply lg:w-1/3;         // 33% en laptops+
  @apply xl:w-1/4;         // 25% en desktops+
}

// En JSX
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">

// ❌ PROHIBIDO: Media queries personalizadas sin justificación
@media (min-width: 900px) {  // ❌ No está en la escala de Tailwind
  .element { width: 50%; }
}
```

---

### 9. 🔝 Z-Index (Capas)

#### Sistema de Z-Index Semántico

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.element {
  @apply z-0;      // 0
  @apply z-10;     // 10 - elementos sobre el contenido
  @apply z-20;     // 20 - overlays
  @apply z-30;     // 30 - modals
  @apply z-40;     // 40 - popovers/tooltips
  @apply z-50;     // 50 - navegación fija
  @apply z-auto;   // auto
}

// Valores negativos (usar con cuidado)
.element {
  @apply -z-10;    // -10 - bajo el contenido
  @apply -z-50;    // -50 - fondos
}

// ❌ PROHIBIDO: Z-index arbitrarios
.element {
  z-index: 999;    // ❌ Usar z-50 o justificar
  z-index: 1;      // ❌ Usar z-10
  z-index: 5;      // ❌ No está en escala de Tailwind
}
```

---

### 10. 🌫️ Opacidades

#### Escala de Opacidad de Tailwind

```scss
// ✅ CORRECTO: Usar clases de Tailwind
.element {
  @apply opacity-0;      // 0%   - invisible
  @apply opacity-5;      // 5%
  @apply opacity-10;     // 10%
  @apply opacity-20;     // 20%
  @apply opacity-25;     // 25%
  @apply opacity-30;     // 30%
  @apply opacity-40;     // 40%
  @apply opacity-50;     // 50%  - semi-transparente
  @apply opacity-60;     // 60%
  @apply opacity-70;     // 70%
  @apply opacity-75;     // 75%
  @apply opacity-80;     // 80%
  @apply opacity-90;     // 90%
  @apply opacity-95;     // 95%
  @apply opacity-100;    // 100% - completamente visible
}

// Opacidad con variables de color (preferido)
.element {
  background-color: hsl(var(--primary) / 0.1);   // 10%
  background-color: hsl(var(--primary) / 0.5);   // 50%
  background-color: hsl(var(--primary) / 0.8);   // 80%
  color: hsl(var(--foreground) / 0.7);           // 70%
}

// ❌ PROHIBIDO: Valores decimales arbitrarios
.element {
  opacity: 0.65;  // ❌ Usar opacity-65 o el valor más cercano
  opacity: 0.33;  // ❌ Usar opacity-30 o opacity-40
}
```

---

## 🛠️ Utilidades Especiales

### Backdrop Filters

```scss
// ✅ CORRECTO: Backdrop blur de Tailwind
.glass {
  @apply backdrop-blur-none;  // 0
  @apply backdrop-blur-sm;    // 4px
  @apply backdrop-blur;       // 8px
  @apply backdrop-blur-md;    // 12px
  @apply backdrop-blur-lg;    // 16px
  @apply backdrop-blur-xl;    // 24px
  @apply backdrop-blur-2xl;   // 40px
  @apply backdrop-blur-3xl;   // 64px
}

// Otros backdrop effects
.element {
  @apply backdrop-brightness-50;   // Oscurecer
  @apply backdrop-brightness-150;  // Aclarar
  @apply backdrop-contrast-125;    // Más contraste
  @apply backdrop-grayscale;       // Blanco y negro
  @apply backdrop-saturate-150;    // Más saturación
}
```

### Filters

```scss
// ✅ CORRECTO: Filtros de Tailwind
.element {
  @apply blur-sm;         // Desenfoque
  @apply brightness-50;   // Brillo
  @apply contrast-125;    // Contraste
  @apply grayscale;       // Escala de grises
  @apply invert;          // Invertir colores
  @apply saturate-150;    // Saturación
  @apply sepia;           // Efecto sepia
}
```

---

## 📋 Checklist de Cumplimiento

### Antes de Escribir Estilos

- [ ] ¿Revisé las clases de Tailwind para esta propiedad?
- [ ] ¿Existe una variable de shadcn/Tailwind para este valor?
- [ ] ¿Verifiqué que NO estoy hardcodeando valores?
- [ ] ¿Puedo usar una clase de utilidad en lugar de CSS personalizado?
- [ ] ¿Este valor funciona en todos los temas (light/dark)?

### Durante Desarrollo

- [ ] ¿Estoy usando `@apply` con clases de Tailwind?
- [ ] ¿Los colores usan `hsl(var(--variable))`?
- [ ] ¿Los espacios usan la escala de Tailwind (0, 1, 2, 3, 4...)?
- [ ] ¿Las transiciones usan duraciones estándar?
- [ ] ¿Los breakpoints usan sm/md/lg/xl/2xl?

### Después de Implementar

- [ ] ¿No hay valores mágicos (números hardcodeados)?
- [ ] ¿Todo se adapta a cambios de tema?
- [ ] ¿El diseño es responsive usando breakpoints estándar?
- [ ] ¿Las sombras y opacidades usan variables HSL?
- [ ] ¿Documenté cualquier excepción necesaria?

---

## 🚫 Anti-Patrones Prohibidos

### ❌ NO HACER NUNCA

```scss
// ❌ Valores hardcodeados en lugar de clases
.component {
  padding: 16px;                    // ❌ Usar @apply p-4
  margin: 8px 12px;                 // ❌ Usar @apply mx-3 my-2
  font-size: 18px;                  // ❌ Usar @apply text-lg
  border-radius: 8px;               // ❌ Usar @apply rounded-lg
  box-shadow: 0 4px 6px rgba(0,0,0,.1);  // ❌ Usar @apply shadow-md
}

// ❌ Colores RGB/HEX directos
.component {
  background-color: #ffffff;        // ❌ Usar hsl(var(--background))
  color: rgb(0, 0, 0);              // ❌ Usar hsl(var(--foreground))
  border-color: #e5e7eb;            // ❌ Usar hsl(var(--border))
}

// ❌ Valores arbitrarios sin justificación
.component {
  padding: 18px;                    // ❌ No está en escala de Tailwind
  font-size: 19px;                  // ❌ Usar text-lg (18px) o text-xl (20px)
  z-index: 999;                     // ❌ Usar z-50
}

// ❌ Mezclar sistemas de espaciado
.component {
  @apply p-4;                       // Tailwind
  margin: 20px;                     // ❌ Hardcoded (usar @apply m-5)
}

// ❌ Media queries personalizadas
@media (min-width: 900px) {         // ❌ Usar lg: (1024px)
  .component { width: 50%; }
}
```

---

## ✅ Patrones Correctos

### Componente Completo con Sistema de Diseño

```scss
.profile-card {
  // Espaciado con escala de Tailwind
  @apply p-6 m-4 gap-4;

  // Colores con variables HSL
  @apply bg-card text-card-foreground;
  @apply border border-border;

  // Border radius con escala de Tailwind
  @apply rounded-2xl;

  // Sombra con clase de Tailwind
  @apply shadow-lg;

  // Transición con duración estándar
  @apply transition-all duration-300;

  // Responsive con breakpoints
  @apply w-full md:w-1/2 lg:w-1/3;

  &__header {
    // Espaciado
    @apply mb-4 pb-4;
    // Borde inferior
    @apply border-b border-border;
  }

  &__title {
    // Tipografía con clases de Tailwind
    @apply text-2xl font-bold text-foreground;
    // Espaciado
    @apply mb-2;
  }

  &__description {
    // Tipografía y color
    @apply text-sm text-muted-foreground leading-relaxed;
  }

  &__actions {
    // Layout
    @apply flex gap-3 mt-6;
  }

  &__button {
    // Colores con variables
    @apply bg-primary text-primary-foreground;
    // Espaciado
    @apply px-6 py-3;
    // Border radius
    @apply rounded-lg;
    // Tipografía
    @apply font-medium;
    // Transición
    @apply transition-all duration-200;
    // Hover con opacidad
    @apply hover:bg-primary/90;

    &--secondary {
      @apply bg-secondary text-secondary-foreground;
      @apply hover:bg-secondary/90;
    }
  }
}
```

### Componente Glass Morphism

```scss
.glass-card {
  // Backdrop effects
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);

  // Fondo con opacidad de variable HSL
  background-color: hsl(var(--card) / 0.7);

  // Borde con opacidad
  @apply border border-border/20;

  // Border radius
  @apply rounded-3xl;

  // Espaciado
  @apply p-6;

  // Sombra adaptativa
  box-shadow:
    0 4px 12px hsl(var(--foreground) / 0.05),
    0 8px 24px hsl(var(--foreground) / 0.08);
}
```

---

## 📍 Ubicación de Archivos del Sistema

```text
src/infrastructure/assets/
├── globals.scss          # Estilos base + reset + variables globales
├── themes.scss           # Definiciones de temas (light/dark/custom)
└── design-tokens.scss    # (Solo si es absolutamente necesario) Tokens adicionales
```

---

## 🔍 Verificación del Sistema

### Comandos de Validación

```bash
# Ver variables CSS del proyecto
grep -r "^[[:space:]]*--" src/infrastructure/assets/

# Buscar valores hardcodeados (requiere revisión manual)
grep -r "px\|rem\|em" src/**/*.scss | grep -v "@apply"

# Buscar colores hex/rgb hardcodeados
grep -r "#[0-9a-fA-F]\{3,6\}\|rgba\?\(" src/**/*.scss
```

---

## 🎓 Recursos y Documentación

### Referencias Oficiales

- [shadcn/ui Theming](https://ui.shadcn.com/docs/theming)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Tailwind Spacing Scale](https://tailwindcss.com/docs/customizing-spacing)
- [Tailwind Colors](https://tailwindcss.com/docs/customizing-colors)
- [Tailwind Typography](https://tailwindcss.com/docs/font-size)
- [Tailwind Border Radius](https://tailwindcss.com/docs/border-radius)
- [Tailwind Shadows](https://tailwindcss.com/docs/box-shadow)

---

## 🧪 Casos de Uso Especiales

### Cuándo Crear Variables Personalizadas

Solo en estos casos excepcionales:

1. **Valores de marca específicos**
   ```scss
   :root {
     /* Logo corporativo que no encaja en primary */
     --brand-accent: 280 70% 55%;
   }
   ```

2. **Estados de dominio específico**
   ```scss
   :root {
     /* Estados de proceso de negocio */
     --status-pending: 45 100% 51%;
     --status-processing: 210 100% 56%;
     --status-completed: 142 76% 36%;
   }
   ```

3. **Integraciones con terceros**
   ```scss
   :root {
     /* Colores de servicio externo */
     --github-green: 94 38% 56%;
     --stripe-purple: 261 51% 51%;
   }
   ```

**Requisitos para variables personalizadas:**

- [ ] Documentar por qué no se puede usar una variable existente
- [ ] Usar formato HSL
- [ ] Definir para todos los temas (light/dark)
- [ ] Justificar su uso en múltiples lugares (3+)
- [ ] Obtener aprobación del equipo

---

## 🔄 Flujo de Decisión Completo

```mermaid
flowchart TD
    A[Necesito aplicar un estilo] --> B{¿Existe clase de Tailwind?}
    B -->|Sí| C[Usar clase de Tailwind con @apply]
    B -->|No| D{¿Es un color?}
    D -->|Sí| E{¿Existe variable HSL de shadcn?}
    E -->|Sí| F[Usar hsl var--variable]
    E -->|No| G{¿Puedo usar con opacidad?}
    G -->|Sí| H[Usar variable existente + opacidad]
    G -->|No| I{¿Es dominio específico?}
    I -->|No| J[Reconsiderar diseño]
    I -->|Sí| K[Crear variable HSL documentada]
    D -->|No| L{¿Es espaciado/tamaño?}
    L -->|Sí| M{¿Está en escala de Tailwind?}
    M -->|Sí| C
    M -->|No| N{¿Es realmente necesario?}
    N -->|No| J
    N -->|Sí| O[Usar valor arbitrario de Tailwind]
```

---

## 📝 Mantra del Sistema de Diseño

> **"Tailwind primero. Variables HSL siempre. Valores hardcodeados nunca. Sistema de diseño centralizado, consistente y dinámico."**

---

## ✨ Validación Final Extendida

Antes de considerar completo cualquier trabajo con estilos:

### Colores
- [ ] ¿Usé solo variables HSL de shadcn (`hsl(var(--variable))`)?
- [ ] ¿No hay valores hex (#...) ni rgb(...) hardcodeados?
- [ ] ¿Los colores funcionan en todos los temas?

### Tipografía
- [ ] ¿Usé clases de Tailwind (`text-*`, `font-*`)?
- [ ] ¿No hay tamaños de fuente en px hardcodeados?
- [ ] ¿Los pesos de fuente usan clases (`font-bold`, etc.)?

### Espaciado
- [ ] ¿Usé la escala de Tailwind (`p-4`, `m-6`, `gap-3`)?
- [ ] ¿No hay padding/margin en px/rem hardcodeados?
- [ ] ¿Los espacios son consistentes con el resto del diseño?

### Bordes y Radius
- [ ] ¿Usé clases de border (`border`, `border-2`)?
- [ ] ¿El border-radius usa clases (`rounded-lg`, `rounded-2xl`)?
- [ ] ¿Los colores de borde usan variables (`border-border`)?

### Sombras
- [ ] ¿Usé clases de shadow (`shadow-md`, `shadow-lg`)?
- [ ] ¿Las sombras personalizadas usan variables HSL?
- [ ] ¿No hay rgba(...) en box-shadow?

### Responsive
- [ ] ¿Usé breakpoints estándar (`sm:`, `md:`, `lg:`, `xl:`)?
- [ ] ¿No hay media queries personalizadas sin justificar?
- [ ] ¿El diseño es mobile-first?

### General
- [ ] ¿No hay valores mágicos (números arbitrarios)?
- [ ] ¿Todo está documentado si es una excepción?
- [ ] ¿El código es mantenible y escalable?
- [ ] ¿Otro desarrollador entendería por qué se hizo así?

**Si alguna respuesta es NO, el trabajo NO está completo.**

---

## 🎯 Objetivo Final

Este sistema garantiza:

- ✅ **Consistencia visual** en toda la aplicación
- ✅ **Temas dinámicos** sin modificar componentes
- ✅ **Mantenibilidad** a largo plazo
- ✅ **Escalabilidad** sin comprometer el diseño
- ✅ **Colaboración efectiva** con un lenguaje común
- ✅ **Rendimiento óptimo** usando clases de Tailwind
- ✅ **Accesibilidad** con contraste adecuado en todos los temas

---

**Fin del documento.**
