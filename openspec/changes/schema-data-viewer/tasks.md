## 1. Schema data viewer hook

- [x] 1.1 Create `use-schema-data-viewer.ts` — load project + datasets, map data through schema (column mapping + transformers + calculated formulas), pagination

## 2. Schema data viewer page

- [x] 2.1 Create `schema-data-viewer-page.tsx` — dataset selector + processed data table
- [x] 2.2 Export from module barrel

## 3. Router + navigation

- [x] 3.1 Create `src/app/projects/[id]/data/page.tsx`
- [x] 3.2 Add "Ver Datos" link to project detail page

## 4. Verification

- [x] 4.1 `tsc --noEmit` — zero errors
- [x] 4.2 `biome check src/` — zero issues
